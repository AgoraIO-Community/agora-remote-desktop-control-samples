{
  "name": "rdc-client-vanilla-ts",
  "version": "0.0.0",
  "main": "build/electron/main.js",
  "private": "true",
  "scripts": {
    "start": "concurrently \"vite\" \"tsc -p electron && cross-env NODE_ENV=development electron .\"",
    "build": "vite build && tsc -p electron && electron-builder"
  },
  "devDependencies": {
    "electron": "^7.3.3",
    "electron-builder": "^22.14.13",
    "typescript": "^4.7.4",
    "vite": "^2.9.12",
    "vite-plugin-commonjs-externals": "^0.1.1"
  },
  "dependencies": {
    "agora-rdc-webrtc-electron": "workspace:^1.0.2",
    "agora-rtc-sdk-ng": "^4.12.0",
    "querystring": "^0.2.1"
  }
}
