name: CI
on: [push, release]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: |
          cd primary && yarn install && cd ../
          cd secondary && yarn install && cd ../

      - name: Build artifacts
        run: |
          cd primary && yarn build:testing && cd ../
          cd secondary && yarn build:testing && cd ../

      - name: Release artifacts
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            primary/dist/**/*.dmg
            primary/dist/**/*.exe
            secondary/dist/**/*.dmg
            secondary/dist/**/*.exe